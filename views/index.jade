extends layout

block content
	h1= title
	p Welcome to #{title}
	pre=JSON.stringify( data, null, 2 )
	table.table
		th
			td Enabled
			td Available
		tr
		each site, idx in data.available
			td=site
			td=data.enabled[idx]
  data
